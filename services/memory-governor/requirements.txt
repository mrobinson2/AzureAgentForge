fastapi>=0.115.0
uvicorn[standard]>=0.32.0
asyncpg>=0.30.0
httpx>=0.27.0
# The watchdog service (bundled in this image, run as a Job) uses sync psycopg2
# for its short-lived agent_events / feature_flags reads.
psycopg2-binary>=2.9.0
