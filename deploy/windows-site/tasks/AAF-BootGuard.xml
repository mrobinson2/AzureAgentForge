<?xml version="1.0" encoding="UTF-8"?>
<!-- Split-brain boot guard (Windows PRIMARY site, WSL2 variant).
     Windows equivalent of com.azureagentforge.boot-guard.plist. Fires at Windows
     startup AND every 5 minutes, invoking the SHARED (cross-platform)
     boot-guard.sh in WSL. The bash script no-ops on any az/Key Vault failure and
     only stops the stack when the lease definitively names another site.
     Placeholders {{REPO}}, {{WSL_DISTRO}}, {{WSL_USER}} are substituted by
     Install-WindowsSite.ps1. -->
<Task version="1.2" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <URI>\AzureAgentForge\AAF-BootGuard</URI>
    <Description>Split-brain boot guard — runs deploy/mac-site/boot-guard.sh in WSL at startup and every 5 minutes (mirrors launchd com.azureagentforge.boot-guard, RunAtLoad + StartInterval 300).</Description>
  </RegistrationInfo>
  <Triggers>
    <BootTrigger>
      <Enabled>true</Enabled>
      <Repetition>
        <Interval>PT5M</Interval>
        <StopAtDurationEnd>false</StopAtDurationEnd>
      </Repetition>
    </BootTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT30M</ExecutionTimeLimit>
    <Priority>7</Priority>
    <RestartOnFailure>
      <Interval>PT1M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>wsl.exe</Command>
      <Arguments>-d {{WSL_DISTRO}} -u {{WSL_USER}} -e bash -lc &quot;{{REPO}}/deploy/mac-site/boot-guard.sh&quot;</Arguments>
    </Exec>
  </Actions>
</Task>
